{% if geojsons %}
    {% for geojson in geojsons %}
        addLayerWKT({{ map }}, '{{ geojson|raw }}');
    {% endfor %}
{% endif %}
{% if coords %}
    addLayerBounds({{ map }}, coords, {{ usePopUp ?? false }});
{% endif %}