{% if "wfs" in datatypes %}
    {% if bboxes|length > 0 %}
        {{ hit_macros.render_fact_title("COMMON.PLACE"|t) }}
        {{ hit_macros.render_entry_as_table("COMMON.PLACE_NORTH"|t, bboxes[0]["northBoundLatitude"]) }}
        {{ hit_macros.render_entry_as_table("COMMON.PLACE_WEST"|t, bboxes[0]["westBoundLongitude"]) }}
        {{ hit_macros.render_entry_as_table("COMMON.PLACE_EAST"|t, bboxes[0]["eastBoundLongitude"]) }}
        {{ hit_macros.render_entry_as_table("COMMON.PLACE_SOUTH"|t, bboxes[0]["southBoundLatitude"]) }}
    {% endif %}
{% endif %}