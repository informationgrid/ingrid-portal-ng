<div>
    <h3>
    {% if query is empty %}
        {{ "COMMON.RESULT_COUNTER_RANKED_GROUPED"|t(search_result.numOfHits) }}
    {% else %}
        {{ "COMMON.RESULT_COUNTER_CATALOG_WITHSEARCHVALUE"|t(search_result.numOfHits, query) }}
    {% endif %}
    </h3>
</div>