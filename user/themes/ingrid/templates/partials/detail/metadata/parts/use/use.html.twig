{# Nutzung #}

<div class="section">
    <a class="anchor" id="{{ hash_use }}"></a>
    {{ hit_macros.render_section_title("SEARCH_DETAIL.USAGE"|t) }}
    <div class="row columns">
{# Nutzungsbedingungen (useConstraints) #}
        {{ hit_macros.render_section_text_label_table_list("SEARCH_DETAIL.OBJECT_ACCESS_LICENSE"|t, useRefs.useConstraints) }}
{# Zugriffsbeschränkungen (accessConstraints) #}
        {{ hit_macros.render_section_text_label_table_list("SEARCH_DETAIL.OBJECT_ACCESS_RESTRICTION_VALUE"|t, useRefs.accessConstraints) }}
{# Anwendungseinschränkungen (useLimitation) #}
        {{ hit_macros.render_section_text_label_table_list("SEARCH_DETAIL.OBJECT_ACCESS_TERMS_OF_USE"|t, useRefs.useLimitations) }}
{# Sicherheitsstufung #}
        {% include 'partials/detail/metadata/parts/use/use_classification_code.html.twig' %}
{# Software #}
        {% include 'partials/detail/metadata/parts/use/use_software.html.twig' %}
    </div>
</div>