{# Nutzungsbedingungen (useConstraints) #}
{% if useRefs.useConstraints %}
    {{ hit_macros.render_section_text_label_list("SEARCH_DETAIL.OBJECT_ACCESS_LICENSE"|t, [useRefs.useConstraints[0]]) }}
{% endif %}