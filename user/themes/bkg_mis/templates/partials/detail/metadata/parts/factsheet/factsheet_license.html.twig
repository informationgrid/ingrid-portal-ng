{# Nutzungsbedingungen (useConstraints) #}
{% if useRefs.useConstraints.constraints %}
    {% set filterEntries = useRefs.useConstraints.constraints|filter(v => not (v starts with '{')) %}
    {{ hit_macros.render_label_list("SEARCH_DETAIL.OBJECT_ACCESS_LICENSE"|t, [filterEntries|first], useRefs.useConstraints.restriction ? useRefs.useConstraints.restriction ~ ":" : null) }}
{% endif %}