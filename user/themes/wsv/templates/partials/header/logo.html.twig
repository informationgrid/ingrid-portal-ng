{% set logo = theme_var('header.logo') %}
{% set header_logo_enabled = theme_var('header.logo_enabled') %}

<div class="row">
    {% if header_logo_enabled %}
    <div class="columns xsmall-12 small-12 medium-8 large-7 xlarge-7">
        <div class="logo" role="banner">
            <a href="{{ home_url }}" title="{{ "PAGES.HOME_MENU_TITLE"|t }}">
            {% if logo %}
                {% set header_logo_file = (logo|first).name %}
            {% else %}
                {% set header_logo_file = 'logo-mobile.png' %}
            {% endif %}
                <img src="{{ url('theme://images/logo/' ~ header_logo_file)  }}" alt="{{ site.title }}" />
            </a>
        </div>
    </div>
    {% endif %}
    <div class="columns">
        <div class="desktop__title">
            <a href="{{ page.find('/home').url }}" title="{{ "PAGES.HOME_MENU_TITLE"|t }}">{{ "PAGES.HEADER_TITLE_MAIN"|t }}</a>
        </div>
    </div>
</div>